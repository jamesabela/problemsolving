The with command both opens and automatically closes the file. (Always try to use this format, it's much safer than older Python.) 

we use a for loop to step through the file. Python knows the length of the file, so you do not have to worry about where the file ends.

we print the line. Notice the ,end="" because the file already has line breaks in it.
